<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!--Where text would appear + the Improved ScrollView -->
    <ScrollView
        android:id="@+id/scrollView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:fillViewport="true">

        <TextView
            android:id="@+id/messageDisplay"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/messages_will_appear_here_hint"
            android:textSize="16sp"
            android:lineSpacingExtra="6dp"
            android:padding="8dp"/>
    </ScrollView>

    <!-- Message Input and Send Button -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="8dp">

        <EditText
            android:id="@+id/messageInput"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:ems="10"
            android:hint="@string/texting_box"
            android:importantForAutofill="no"
            android:inputType="textAutoCorrect"
            android:maxLines="1"
            tools:ignore="ContentDescription,TouchTargetSizeCheck,VisualLintTextFieldSize" /> <!-- Optional improvements -->

        <Button
            android:id="@+id/sendButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/send_bttn"
            android:minWidth="100dp"
            android:layout_marginStart="8dp"
            tools:ignore="VisualLintBounds" />
    </LinearLayout>

    <!-- Button to Open InfoActivity -->
    <Button
        android:id="@+id/infoButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/view_network_info_bttn"
        tools:ignore="VisualLintButtonSize"
        android:layout_marginTop="8dp" /> <!-- Optional improvement -->
</LinearLayout>